spring:
  batch:
    job:
      enabled: true
    jdbc:
      initialize-schema: never
  datasource:
    url: ""
    driver-class-name: ""
  config:
    import:
      - optional:application-module_dev.yml
  main:
#    allow-bean-definition-overriding: true
    lazy-initialization: true
custom:
  app:
    message: "Application.yml Development config"